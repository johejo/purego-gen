/*
 * Basic fixture declarations for smoke tests that exercise func/type emission.
 * Identifier spellings are part of stable golden outputs.
 */
typedef int my_int;
typedef void* my_handle;
typedef struct not_basic not_basic;
int add(int lhs, int rhs);
void reset(void);
